- comment: Requirement Diagram
  begin: ^\s*(requirementDiagram)
  beginCaptures:
    '1':
      name: keyword.control.mermaid
  patterns:
    - match: \%%.*
      name: comment
  end: (^|\G)(?=\s*[`:~]{3,}\s*$)
