- comment: Mindmap Diagram
  begin: ^\s*(mindmap)
  beginCaptures:
    '1':
      name: keyword.control.mermaid
  patterns:
    - match: \%%.*
      name: comment
    - match: ^(\s*["$&\^/#.,?!;:*+=<>\'\\\-\w\s]*)
      name: string
  end: (^|\G)(?=\s*[`:~]{3,}\s*$)
